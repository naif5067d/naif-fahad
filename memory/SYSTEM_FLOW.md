# 📚 دليل نظام الحضور والموارد البشرية الشامل
# ============================================================

## 🔄 قصة حياة الموظف (من العقد إلى المخالصة)

```
┌─────────────────────────────────────────────────────────────────┐
│  1️⃣  إنشاء العقد (contracts_v2)                                  │
│     └─ status: "active"                                         │
│     └─ work_start_date: تاريخ بدء العمل                         │
│     └─ sandbox_mode: فترة تجريبية (اختياري)                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  2️⃣  ربط الموظف بموقع العمل (work_locations)                     │
│     └─ work_days: {saturday: true, sunday: true, ...}           │
│     └─ work_start: "08:00" / work_end: "17:00"                  │
│     └─ grace_checkin_minutes: 15 (فترة السماح للتأخير)          │
│     └─ grace_checkout_minutes: 15 (فترة السماح للخروج المبكر)    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  3️⃣  العمل اليومي                                                │
│                                                                 │
│  📱 البصمة الذاتية (attendance_ledger):                          │
│     └─ check_in: تسجيل الحضور بـ GPS                            │
│     └─ check_out: تسجيل الانصراف بـ GPS                         │
│                                                                 │
│  📋 المعاملات (transactions):                                    │
│     └─ leave_request: طلب إجازة                                 │
│     └─ permission: استئذان (ساعات)                              │
│     └─ mission: مهمة خارجية                                     │
│     └─ late_excuse: تبرير تأخير                                 │
│     └─ early_leave_excuse: تبرير خروج مبكر                      │
│     └─ forgotten_punch: نسيان بصمة                              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  4️⃣  التحليل اليومي (Day Resolver V2)                            │
│                                                                 │
│  ترتيب الفحص (بالتسلسل):                                         │
│   1. holiday    → هل اليوم عطلة رسمية؟                          │
│   2. weekend    → هل اليوم عطلة نهاية أسبوع؟ (من work_days)      │
│   3. leave      → هل الموظف في إجازة منفذة؟                     │
│   4. mission    → هل الموظف في مهمة خارجية؟                     │
│   5. forget     → هل هناك نسيان بصمة معتمد؟                     │
│   6. attendance → هل سجّل بصمة؟ (تحليل تأخير/خروج مبكر)         │
│   7. permission → هل هناك استئذان؟                              │
│   8. excuses    → هل هناك تبريرات معتمدة؟                       │
│   9. ABSENT     → غياب بدون عذر                                 │
│                                                                 │
│  النتيجة → daily_status                                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  5️⃣  الحالات الممكنة (DailyStatusEnum)                           │
│                                                                 │
│  ✅ PRESENT       - حاضر                                         │
│  ⏰ LATE          - متأخر (تجاوز فترة السماح)                    │
│  🚪 EARLY_LEAVE   - خروج مبكر                                    │
│  🏖️ ON_LEAVE      - إجازة                                        │
│  📋 ON_ADMIN_LEAVE - إجازة إدارية                                │
│  🚗 ON_MISSION    - مهمة خارجية                                  │
│  📝 PERMISSION    - استئذان                                      │
│  🎉 HOLIDAY       - عطلة رسمية                                   │
│  📅 WEEKEND       - عطلة نهاية أسبوع                             │
│  ❌ ABSENT        - غائب                                         │
│  ⏰ LATE_EXCUSED  - تأخير معذور                                  │
│  🚪 EARLY_EXCUSED - خروج مبكر معذور                              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  6️⃣  الخصومات والإنذارات                                         │
│                                                                 │
│  📉 deduction_proposals:                                         │
│     └─ pending → reviewed → approved → executed                 │
│     └─ سلطان/نايف يراجعون، ستاس ينفذ                            │
│                                                                 │
│  ⚠️ warning_ledger:                                              │
│     └─ 5 أيام غياب متفرقة = إنذار أول                           │
│     └─ 10 أيام = إنذار ثاني                                     │
│     └─ 15 يوم متتالي = فصل                                      │
│     └─ 30 يوم متفرق = فصل                                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  7️⃣  الحساب الشهري (monthly_hours)                               │
│                                                                 │
│     └─ required_hours: الساعات المطلوبة                         │
│     └─ actual_hours: الساعات الفعلية                            │
│     └─ permission_hours: ساعات الاستئذان                         │
│     └─ net_hours: الصافي                                        │
│     └─ deficit_hours: العجز                                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  8️⃣  إنهاء العقد والمخالصة                                       │
│                                                                 │
│  📜 contracts_v2.status = "terminated"                           │
│                                                                 │
│  💰 المخالصة (settlements):                                      │
│     └─ التحقق: عهد مرجعة؟ سلف مسددة؟                            │
│     └─ الحساب: مكافأة نهاية الخدمة + بدل إجازات                 │
│     └─ finance_ledger: تسجيل المخالصة                           │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 أين تُعرض الحالات؟

| المكان | الوصف | الملف |
|--------|-------|-------|
| **لوحتي** | الموظف يرى حضوره ومعاملاته | `MyDashboard.js` |
| **صفحة الحضور** | الإدارة ترى جميع الموظفين | `TeamAttendancePage.js` |
| **مرآة ستاس** | العروق والتفاصيل الكاملة | `STASMirrorPage.js` |

## ⏰ حساب التأخير

```python
# من day_resolver_v2.py

work_start = "08:00"  # من موقع العمل
grace_period = 15     # فترة السماح (دقائق)

# وقت السماح الفعلي
allowed_late = work_start + grace_period  # 08:15

# إذا سجّل الحضور بعد 08:15
if check_in_time > allowed_late:
    is_late = True
    late_minutes = check_in_time - work_start  # عدد الدقائق
```

## 🗓️ كيف يعرف النظام عطلة نهاية الأسبوع؟

```python
# من work_locations
work_days = {
    "saturday": True,   # يوم عمل
    "sunday": True,     # يوم عمل
    "monday": True,     # يوم عمل
    "tuesday": True,    # يوم عمل
    "wednesday": True,  # يوم عمل
    "thursday": True,   # يوم عمل
    "friday": False     # عطلة ← إذا False = عطلة نهاية أسبوع
}

# كل موقع له أيام عمل مختلفة
# مثال: موظف إجازته خميس + جمعة + سبت
work_days = {
    "thursday": False,
    "friday": False,
    "saturday": False,
    "sunday": True,
    ...
}
```

## 🕐 متى يُرصد الغياب؟

### 1. الرصد التلقائي (Jobs)
```
/api/attendance-engine/jobs/daily
```
- يُشغّل يومياً (آخر اليوم أو صباح اليوم التالي)
- يحلل جميع الموظفين
- ينشئ `daily_status` لكل موظف
- ينشئ `deduction_proposals` للغياب

### 2. التحضير اليدوي (من الإدارة)
```
/api/attendance-engine/process-daily
```
- **لا يُغيّر البصمة الذاتية GPS** ← الأصل دائماً الذاتي
- فقط يُحلل من لم يُسجل
- يُستخدم للإضافة وليس للتعديل

## 📝 تبرير التأخير

### متى يستطيع الموظف تبرير التأخير؟
1. **نسيان بصمة**: `forgotten_punch` - حد 3 شهرياً
2. **تبرير تأخير**: `late_excuse` - يحتاج موافقة
3. **تبرير خروج مبكر**: `early_leave_excuse` - يحتاج موافقة

### مسار التبرير:
```
موظف يُنشئ طلب → pending
    ↓
مشرف/إدارة يراجع → approved / rejected
    ↓
إذا approved → executed
    ↓
النظام يُعيد تحليل اليوم ويُحدث daily_status
```

## 🔒 نظام القفل (Lock System)

```python
lock_status:
  - "open": مفتوح للتعديل (48 ساعة)
  - "review": تحت المراجعة
  - "locked": مقفل نهائياً (لا يمكن التعديل)
```

## 🏷️ الجداول الرئيسية

| الجدول | الوصف |
|--------|-------|
| `employees` | بيانات الموظفين |
| `contracts_v2` | العقود |
| `work_locations` | مواقع العمل وأيام الدوام |
| `attendance_ledger` | سجل البصمات |
| `daily_status` | حالة كل يوم لكل موظف |
| `transactions` | المعاملات (إجازات، استئذان، مهمات) |
| `holidays` | العطل الرسمية |
| `monthly_hours` | الملخص الشهري |
| `deduction_proposals` | مقترحات الخصم |
| `finance_ledger` | السجل المالي |
| `warning_ledger` | الإنذارات |
| `settlements` | المخالصات |

## ✅ التأكد من أن النظام يعمل

### 1. فحص العقد
```bash
curl /api/contracts/employee/{employee_id}
# يجب أن يرجع status: "active"
```

### 2. فحص موقع العمل
```bash
curl /api/work-locations/{location_id}
# يجب أن يحتوي work_days, work_start, work_end
```

### 3. تسجيل البصمة
```bash
curl /api/attendance/check-in
# يجب أن يكون GPS متاح والموقع صحيح
```

### 4. تحليل اليوم
```bash
curl /api/attendance-engine/daily-status/{employee_id}/{date}
# يرجع الحالة + العروق الكاملة
```
