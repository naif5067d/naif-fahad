# HR Management System - PRD

## Original Problem Statement
نظام إدارة موارد بشرية شامل يتضمن:
- إدارة الموظفين والعقود
- نظام الحضور والانصراف مع GPS
- إدارة الإجازات والأذونات
- نظام المعاملات والموافقات
- لوحة تحكم إدارية (STAS Mirror)
- نظام العهدة المالية الإدارية

## Core Architecture
- **Backend**: FastAPI + MongoDB
- **Frontend**: React + Shadcn/UI + TailwindCSS
- **Database**: MongoDB (test_database)
  - العقود في جدول `contracts_v2`
  - الموظفين في جدول `employees`
  - العهد المالية في جدول `admin_custodies`
  - مصروفات العهد في جدول `custody_expenses`
  - أكواد المصروفات في جدول `expense_codes`
  - إعدادات التوقيعات في جدول `settings` (type: custody_signatures)

## What's Been Implemented

### 2026-02-24 (Session 5 - Part 2) - تحسينات PDF والأكواد ✅

1. **إعادة كتابة ملف PDF بالكامل** ✅
   - ترويسة الشركة في كل صفحة مع الشعار
   - إطار جانبي احترافي (Navy + Gold)
   - ترقيم الصفحات تلقائي
   - تنسيق مثل Excel (جداول نظيفة ومريحة)
   - ألوان الشركة الرسمية
   - التوقيعات الثلاثية:
     - أ.سلطان الزامل - المدير الإداري
     - أ.صلاح صحبي - المحاسب المالي
     - م.محمد الثنيان - المدير التنفيذي

2. **إصلاح تغيير الشهر في التقرير** ✅
   - المشكلة: regex لم يكن يعمل بشكل صحيح
   - الحل: استخدام date range ($gte, $lt) للبحث

3. **تعديل وحذف الأكواد (61+)** ✅
   - إضافة زر تعديل للأكواد الجديدة
   - إضافة زر حذف (مع التحقق من الاستخدام)
   - حماية الأكواد الأساسية (1-60)

4. **إعدادات التوقيعات في STAS** ✅
   - تبويب جديد "التوقيعات" في STAS Mirror
   - تعديل أسماء ومناصب الموقعين
   - الإعدادات تنعكس في PDF

### 2026-02-24 (Session 5 - Part 1) - إصلاحات العهدة المالية ✅

1. **فصل البيان عن الوصف في المصروفات (P0)** ✅
   - **المشكلة**: عند إضافة كود جديد (61+)، كان البيان والوصف يُحفظان بنفس القيمة
   - **الحل**: إضافة حقل `statement` منفصل في الفورم
   - **النتيجة**: 
     - البيان (اسم الحساب) = اسم الكود الجديد في جدول expense_codes
     - الوصف = سبب الصرف الفعلي في سجل المصروف

### 2026-02-24 (Session 4) - تحسينات سابقة

1. **شاشة مراقبة النظام الحية (System Monitoring)** ✅
   - **المتطلب**: إضافة معلومات تشغيل حقيقية في صفحة صيانة النظام
   - **الميزات المنجزة**:
     - عرض RAM: الإجمالي، المستخدم، النسبة المئوية (%)
     - عرض CPU: نسبة الاستهلاك الحالية وعدد الأنوية
     - عرض Storage: المساحة الإجمالية، المتاحة، المستخدمة (GB)
     - تحليل الملفات: إجمالي الملفات، ملفات ATS، ملفات المعاملات، أخرى (MB)
     - مدة التشغيل (Uptime): يوم/ساعة/دقيقة/ثانية
     - معلومات العملية: ذاكرة، CPU، عدد الخيوط
     - تحديث تلقائي كل 30 ثانية
   - **الملفات المعدلة**:
     - `/app/backend/routes/maintenance.py` - إضافة endpoint `/api/maintenance/system-metrics`
     - `/app/frontend/src/pages/SystemMaintenancePage.js` - إضافة تبويب "المراقبة"
   - **تثبيت**: مكتبة `psutil` للحصول على معلومات النظام

2. **عرض حدود Kubernetes الفعلية** ✅
   - **المتطلب**: عرض الحدود المخصصة للـ Pod وليس موارد الـ host
   - **الميزات المنجزة**:
     - قراءة حدود cgroup v2 الفعلية
     - عرض Memory Limit: 8 GB
     - عرض CPU Limit: 2 Cores (2000 millicores)
     - عرض Ephemeral Storage: 9.75 GB
     - عرض Database Storage: 9.75 GB
     - عرض File Upload Max: 100 MB
     - عرض سلوك التجاوز لكل مورد (OOMKilled, Throttle, etc.)
     - جدول مفصل للحدود مع النسب والحالة
   - **الملفات المعدلة**:
     - `/app/backend/routes/maintenance.py` - دالة `_get_kubernetes_limits()`
     - `/app/frontend/src/pages/SystemMaintenancePage.js` - جدول الحدود المفصل

### 2026-02-22 (Session 3)

1. **إصلاح مشكلة عدم تحديث رقم الإصدار في Footer (P0)** ✅
   - **السبب الجذري**: `/api/health` كان يُرجع إصدار ثابت من `APP_VERSION` في `server.py`
   - **الحل**: تعديل `/api/health` ليقرأ الإصدار من قاعدة البيانات (collection: `settings`, type: `app_version`)
   - **الملف المُعدّل**: `/app/backend/server.py`
   - الآن عند تحديث الإصدار من STAS Mirror ← يظهر فوراً في Dashboard

### 2026-02-21 (Session 2)

1. **إصلاح التوقيت (P0)** ✅
   - تحويل جميع أوقات الحضور من UTC إلى توقيت الرياض (+03:00)

2. **إصلاح مشكلة "لا يوجد عقد نشط" (P0)** ✅
   - **السبب الجذري**: كان الكود يبحث بشرط `is_active: True`
   - العقود في `contracts_v2` لديها `is_active: None`
   - **الملفات المصلحة**:
     - `/app/backend/utils/attendance_rules.py`
     - `/app/backend/services/day_resolver_v2.py`

3. **ميزة تحديث الإصدار** ✅
   - استبدال "حذف المعاملات" بـ "إدارة الإصدار" في STAS Mirror

4. **ربط الموظفين بموقع العمل** ✅
   - جميع الموظفين الآن مربوطين بالمقر الرئيسي

## System Flow (قصة حياة الموظف)

راجع الملف: `/app/memory/SYSTEM_FLOW.md`

### ترتيب الفحص اليومي (Day Resolver V2):
```
1. holiday    → عطلة رسمية
2. weekend    → عطلة نهاية أسبوع (من work_days في موقع العمل)
3. leave      → إجازة منفذة
4. mission    → مهمة خارجية
5. forget     → نسيان بصمة معتمد
6. attendance → بصمة فعلية (تحليل تأخير/خروج مبكر)
7. permission → استئذان
8. excuses    → تبريرات
9. ABSENT     → غياب
```

### كيف يُحدد التأخير:
```python
work_start = "10:00"           # من موقع العمل
grace_period = 10              # فترة السماح (دقائق)
allowed_late = "10:10"         # آخر وقت مسموح

if check_in > allowed_late:
    is_late = True
    late_minutes = check_in - work_start
```

### كيف تُحدد عطلة نهاية الأسبوع:
- من حقل `work_days` في موقع العمل
- إذا `friday: false` → الجمعة عطلة
- كل موظف حسب موقع عمله

## API Endpoints المهمة

| Endpoint | الوصف |
|----------|-------|
| `POST /api/attendance/check-in` | تسجيل حضور |
| `POST /api/attendance/check-out` | تسجيل انصراف |
| `GET /api/attendance-engine/daily-status/{emp_id}/{date}` | تحليل يوم + العروق |
| `POST /api/attendance-engine/process-daily` | التحضير اليدوي |
| `GET /api/settings/version` | إصدار التطبيق |

## Test Credentials
- Admin: `stas` / `123456`
- Employee: `16` / `123456` (محمد فوزي)
- Employee: `10` / `123456` (وليد صابر)

## Prioritized Backlog

### P0 (Critical) - COMPLETED ✅
- [x] إصلاح التوقيت
- [x] إصلاح "لا يوجد عقد نشط"
- [x] شرح آلية النظام

### P1 (High)
- [ ] دمج صفحتي "ماليتي" و "الحضور/الجزاءات"
- [ ] واجهة التقييم السنوي للأداء

### P2 (Medium)
- [ ] نظام القروض
- [ ] إظهار الميزات المخفية في الواجهة
- [ ] تحسين صلاحيات المشرفين

### 2026-02-21 (Session 3) - UI/UX Theme Enforcement ✅

**المهمة الرئيسية:** تطبيق شامل لألوان الشركة وإزالة جميع الإيموجي والأيقونات غير المهنية

#### التغييرات المنجزة:

1. **استبدال أيقونة HandMetal بـ FileSignature** ✅
   - الأيقونة السابقة كانت غير مهنية
   - الملفات المعدلة: `TeamAttendancePage.js`

2. **إزالة جميع الإيموجي من الواجهات** ✅
   - إزالة: 📂, ⏳, ✅, 🔒, 🔔, 📅, ✍️, ⚠️, 👥
   - استبدالها بأيقونات Lucide-react احترافية

3. **تطبيق ألوان الشركة** ✅
   - تصحيح 342+ انتهاك للألوان
   - الألوان المعتمدة:
     - Navy: `hsl(222, 47%, 24%)` - #1E3A5F
     - Lavender: `hsl(262, 83%, 76%)` - #A78BFA
     - Success: `hsl(160, 84%, 39%)`
     - Warning: `hsl(38, 92%, 50%)`
     - Info: `hsl(217, 91%, 60%)`
     - Destructive: `hsl(0, 84%, 60%)`

4. **الملفات المعدلة الرئيسية:**
   - `/app/frontend/src/pages/TeamAttendancePage.js`
   - `/app/frontend/src/pages/AdminCustodyPage.js`
   - `/app/frontend/src/pages/STASMirrorPage.js`
   - `/app/frontend/src/pages/LoginPage.js`
   - `/app/frontend/src/pages/DashboardPage.js`
   - `/app/frontend/src/pages/LeavePage.js`
   - `/app/frontend/src/pages/ContractsManagementPage.js`
   - `/app/frontend/src/components/NotificationBell.js`
   - `/app/frontend/src/components/Timeline.js`
   - `/app/frontend/src/components/ErrorAlert.js`
   - `/app/frontend/src/App.css`
   - جميع الصفحات والمكونات الأخرى

5. **نتائج الاختبار:**
   - 8/8 اختبارات أساسية ناجحة
   - نسبة النجاح: 100%

### 2026-02-22 - نظام تتبع المتقدمين (ATS) ✅

**المهمة الرئيسية:** بناء نظام ATS كامل ومستقل عن النظام الرئيسي

#### الميزات المنجزة:

1. **العزل الأمني الكامل** ✅
   - قاعدة بيانات منفصلة لتخزين البيانات
   - مجلد تخزين مستقل للملفات: `/app/ats_storage/cv_files`
   - لا يوجد أي تداخل مع بيانات الموارد البشرية الرئيسية

2. **واجهة الإدارة (Admin)** ✅
   - إنشاء الوظائف وتعديلها وإغلاقها
   - عرض الطلبات مع التصنيف الذكي (Tier A/B/C)
   - نظام الملاحظات والتقييم
   - إحصائيات شاملة

3. **البوابة العامة (Public)** ✅
   - صفحة `/careers` لعرض جميع الوظائف النشطة
   - نموذج تقديم مع رفع ملفات (PDF/DOC/DOCX)
   - رسائل خطأ واضحة بالعربية والإنجليزية

4. **المحرك الذكي (Phase 2)** ✅
   - استخراج النص من ملفات السيرة الذاتية
   - تقييم آلي (0-100) مع تصنيف (Excellent/Strong/Acceptable/Weak)
   - كشف الحشو (Fluff) والأنا (Ego)
   - علامة "High Potential" للمواهب الواعدة

5. **الحذف النووي (Nuclear Delete)** ✅ - 2026-02-22
   - متاح فقط لـ `stas` و `sultan`
   - حذف جميع الطلبات والملفات بشكل نهائي
   - حوار تأكيد مع كتابة "DELETE ALL"
   - تسجيل العمليات في audit log

#### الملفات الرئيسية:
- **Backend:**
  - `/app/backend/routes/ats_admin.py` - API الإدارة
  - `/app/backend/routes/ats_public.py` - API العامة
  - `/app/backend/services/ats_scoring.py` - محرك التقييم
  - `/app/backend/services/ats_extraction.py` - استخراج النصوص

- **Frontend:**
  - `/app/frontend/src/pages/ATSPage.js` - صفحة الإدارة
  - `/app/frontend/src/pages/CareersPage.js` - البوابة العامة
  - `/app/frontend/src/pages/PublicApplyPage.js` - نموذج التقديم

#### صلاحيات ATS:
| المستخدم | الوصول | الحذف النووي |
|----------|--------|--------------|
| stas | ✅ | ✅ |
| sultan | ✅ | ✅ |
| naif | ✅ | ❌ |
| mohammed | ✅ | ❌ |
| HR Role | ✅ | ❌ |

---

### 2026-02-22 (Session 4) - نظام الساعات الذكية (Smart Hours) ✅

**المهمة الرئيسية:** تنفيذ نظام سماح التعويض الشهري

#### المفهوم:
- **ساعات الاستئذان (2-3 ساعات/شهر):** حق نظامي للموظف، لا تُخصم من راتبه، مشروطة بالموافقة
- **سماح التعويض (0-50 ساعة/شهر):** إعداد إداري خاص، الموظف لا يراه، يسمح بتعويض التأخيرات بالساعات الإضافية

#### الميزات المنجزة:

1. **API سماح التعويض** ✅
   - `GET /api/settings/compensation-allowance` - جلب الإعداد (للمدراء فقط)
   - `PUT /api/settings/compensation-allowance` - تحديث الإعداد

2. **واجهة المستخدم** ✅
   - قسم جديد في أعلى صفحة مواقع العمل
   - Slider لاختيار قيمة سماح التعويض (0-50 ساعة)
   - أزرار سريعة للاختيار (0, 5, 10, 20, 30, 50)
   - الموظف العادي لا يرى هذا القسم

3. **حساب الخصومات الذكي** ✅
   - حساب النقص الإجمالي (تأخير + خروج مبكر)
   - حساب الساعات الإضافية (البقاء بعد الدوام)
   - الخصم الصافي = النقص - MIN(الساعات الإضافية, سماح التعويض)
   - شرح مفصل للحساب في `calculation_note_ar`

#### الملفات المعدلة:
- `backend/routes/settings.py` - إضافة API سماح التعويض
- `backend/services/penalty_service.py` - منطق الساعات الذكية
- `frontend/src/pages/WorkLocationsPage.js` - واجهة سماح التعويض

---

### 2026-02-22 (Session 4) - نظام السياسات والحوكمة ✅

**المهمة الرئيسية:** بناء دفتر حوكمة ذكي (Smart Governance Booklet)

#### الميزات المنجزة:

1. **نظامان رئيسيان** ✅
   - النظام العام (Public System)
   - النظام الخاص (Private System) - تصميم أكثر رسمية

2. **إدارة الفصول** ✅
   - إنشاء فصول جديدة
   - تحرير العناوين والمحتوى
   - حفظ كمسودة / نشر
   - إعادة ترتيب الفصول (Drag & Drop)
   - حذف الفصول

3. **المحرر الذكي (TipTap)** ✅
   - Rich Text Editor
   - تنسيق النص (Bold, Italic, Underline)
   - العناوين (H1, H2, H3)
   - القوائم المرقمة والنقطية
   - الجداول
   - الفواصل
   - كتل الاقتباس
   - كتل التحذير والملاحظات
   - ألوان النص والتظليل
   - دعم RTL/LTR

4. **التذييل القانوني** ✅
   - يظهر أسفل كل فصل
   - قابل للتعديل من STAS فقط
   - نص عربي وإنجليزي

5. **الصلاحيات** ✅
   - المدراء (stas, sultan, naif): إنشاء، تحرير، نشر، حذف
   - الموظفون: عرض المنشور فقط

#### الملفات الجديدة:
- `backend/routes/policies.py` - API السياسات
- `frontend/src/pages/PoliciesPage.js` - صفحة السياسات
- `frontend/src/components/SmartEditor.jsx` - المحرر الذكي

#### قاعدة البيانات:
- `policy_chapters` - الفصول
- `settings.policy_legal_footer` - التذييل القانوني

---

## Prioritized Backlog (Updated)

### P0 (Critical) - COMPLETED ✅
- [x] إصلاح التوقيت
- [x] إصلاح "لا يوجد عقد نشط"
- [x] شرح آلية النظام
- [x] نظام ATS - المرحلة الأولى
- [x] نظام ATS - المرحلة الثانية (الذكي)
- [x] ميزة الحذف النووي للـ ATS
- [x] نظام الساعات الذكية (Smart Hours)
- [x] نظام السياسات والحوكمة (Policies Module)

### P1 (High)
- [ ] تأكيد إصلاح iframe للـ ATS من موقع Wuilt
- [ ] دمج صفحتي "ماليتي" و "الحضور/الجزاءات"
- [ ] واجهة التقييم السنوي للأداء
- [ ] تقرير المعايرة الشهري للـ ATS

### P2 (Medium)
- [ ] نظام القروض
- [ ] إظهار الميزات المخفية في الواجهة
- [ ] تحسين صلاحيات المشرفين
- [ ] ميزة البحث بالباركود والكاميرا


### 2026-02-23 - نظام PDF الاحترافي الموحد ✅

**المهمة الرئيسية:** إعادة تصميم جميع مستندات PDF بتصميم احترافي موحد مع خط قطع للملفات اليدوية

#### الميزات المنجزة:

1. **الترويسة الموحدة** ✅
   - ثنائية اللغة (عربي يمين، إنجليزي يسار)
   - شعار الشركة في المنتصف
   - بيانات الشركة الرسمية (رقم الترخيص، السجل التجاري)

2. **خط القطع (Tear-off Line)** ✅
   - نص ثنائي اللغة: `Cut Here | قص هنا`
   - رمز المقص ✂
   - خط منقط للفصل

3. **جدول التوقيعات المنظم** ✅
   - كل موقع في خانة منفصلة
   - QR Code لكل توقيع
   - الاسم بالعربي والإنجليزي
   - تاريخ التوقيع
   - حالة التوقيع (✓ تم التوقيع)

4. **QR Code لـ STAS مزدوج** ✅
   - QR في جدول التوقيعات
   - QR إضافي في قسم الكوبون للملفات اليدوية

5. **قسيمة القطع (Coupon)** ✅
   - اسم الشركة بالعربي والإنجليزي
   - نوع المستند
   - رقم المرجع
   - اسم الموظف
   - QR للتحقق
   - QR لـ STAS للملفات اليدوية
   - نص "معاملة صحيحة - Valid Document" للمنفذة

6. **API جديد لتحميل PDF** ✅
   - `GET /api/stas/transaction/{id}/pdf`
   - يرجع PDF مع headers:
     - `X-Integrity-ID`: معرف السلامة
     - `X-PDF-Hash`: هاش المستند

#### الملفات الجديدة/المعدلة:
- `backend/utils/professional_pdf.py` - مولد PDF الاحترافي الجديد
- `backend/utils/inkind_custody_pdf.py` - تحديث ليستخدم التصميم الجديد
- `backend/routes/stas.py` - إضافة endpoint تحميل PDF

#### نتائج الاختبار:
- 16/16 اختبار ناجح
- PDF صالح بحجم ~125KB
- 2 صفحات
- 4 QR codes

#### تفاصيل PDF:
```
الصفحة 1:
├── الترويسة الموحدة (عربي + إنجليزي)
├── عنوان المستند (عربي + إنجليزي)
├── رقم المرجع
├── معلومات المعاملة (جدول ثنائي اللغة)
├── تفاصيل المعاملة
└── جدول التوقيعات (مع QR لكل موقع)

═════════════════════════════════════
       ✂ Cut Here | قص هنا ✂
═════════════════════════════════════

الصفحة 2 (الكوبون):
├── QR STAS (للملفات اليدوية)
├── اسم الشركة
├── نوع المستند
├── رقم المرجع
├── اسم الموظف
├── QR للتحقق
└── حالة المستند (معاملة صحيحة)
```

---

### 2026-02-23 - إصلاح PDF والأسماء ✅

**المهمة الرئيسية:** إصلاح عرض الأسماء والأدوار في جدول توقيعات PDF

#### المشاكل المُصلحة:

1. **إصلاح عرض الدور "المدير الإداري" بدلاً من "المشرف" (P1)** ✅
   - **السبب الجذري**: كان `role_definitions` يستخدم mapping ثابت خاطئ
   - **الحل**: تحديث mapping الأدوار ليعرض "المدير الإداري" لـ `ops`
   - **الملف المُعدّل**: `/app/backend/utils/professional_pdf.py`

2. **إصلاح عرض الأسماء العربية والإنجليزية (P1)** ✅
   - **السبب الجذري**: `approval_chain` لم يحتوِ على `approver_name_ar`
   - **الحل**:
     - تحديث `transactions.py` ليخزن كل من `approver_name_ar` و `approver_name_en`
     - تحديث endpoint الـ PDF ليجلب الأسماء من قاعدة البيانات للمعاملات القديمة
   - **الملفات المُعدّلة**:
     - `/app/backend/routes/transactions.py`
     - `/app/backend/utils/professional_pdf.py`

3. **النتيجة في PDF:**
   - العنوان: "المدير الإداري" / "Admin Manager"
   - الاسم العربي: "أ.سلطان الزامل" (بخط كبير)
   - الاسم الإنجليزي: "Mr.Sultan Al Zamil" (بخط صغير تحته)
   - STAS: يظهر بالأزرق الداكن

#### ملاحظة حول حساب أيام العطل:
- الكود يعمل بشكل صحيح في Preview
- إذا ظهرت الأيام مضاعفة في Production، فالمشكلة في البيانات (سجلات مكررة) وليس في الكود

---

## Prioritized Backlog (Updated 2026-02-23)

### P0 (Critical) - COMPLETED ✅
- [x] إصلاح PDF - عرض الدور الصحيح "المدير الإداري"
- [x] إصلاح PDF - عرض الأسماء العربية والإنجليزية

### P1 (High)
- [ ] دمج صفحتي "ماليتي" و "الحضور/الجزاءات"
- [ ] واجهة التقييم السنوي للأداء
- [ ] محرر ذكي للسياسات (مثل Canva)
- [ ] السماح لـ STAS بإدارة هوية التطبيق

### P2 (Medium)
- [ ] نظام القروض
- [ ] إضافة صور للعهد العينية
- [ ] التحقق من عمل الماسح الضوئي QR

### P3 (Low)
- [ ] تحسين استجابة الموبايل
- [ ] الإجابة على سؤال حساب الخصومات وساعات التأخير

